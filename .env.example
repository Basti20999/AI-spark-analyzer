# You only need this file for the API backend (--backend api).
# If you have a Claude Pro/Max subscription, you do NOT need an API key:
# install Claude Code, run `claude login`, and use --backend cli instead.
#
# Copy to .env and fill in, or export in your shell.
ANTHROPIC_API_KEY=sk-ant-...
